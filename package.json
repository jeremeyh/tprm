{
  "name": "tprm-dm-redirect-bot",
  "version": "2.0.0",
  "description": "DM-only auto-reply (in-thread) using user tokens, with availability toggles via slash command.",
  "main": "src/app.js",
  "scripts": {
    "dev": "nodemon --watch src --ext js,json --exec \"node ./src/app.js\"",
    "start": "node ./src/app.js"
  },
  "dependencies": {
    "@slack/bolt": "^3.18.0",
    "@slack/web-api": "^6.13.0",
    "dotenv": "^16.6.1",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}